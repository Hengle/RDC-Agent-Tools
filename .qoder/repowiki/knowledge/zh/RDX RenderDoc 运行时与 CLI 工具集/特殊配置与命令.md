- **入口点**：支持 `rdx.bat` (Windows), `bin/rdx` (POSIX), 或 `python cli/run_cli.py` 启动。
- **守护进程管理**：CLI 自动触发 Daemon 启动，支持 `rdx daemon start/stop/status` 手动干预。
- **Smoke 测试**：通过 `bash scripts/smoke_cli.sh` 执行端到端验证，支持传入外部 `.rdc` 文件进行全链路测试。