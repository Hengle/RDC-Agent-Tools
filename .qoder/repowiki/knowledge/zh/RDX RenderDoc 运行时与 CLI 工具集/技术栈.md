- **Python 3.11+** 运行时，依赖 `pydantic>=2.0` 进行数据校验，`aiofiles` 处理异步 I/O。
- **RenderDoc Integration**：通过 `binaries/windows/x64/pymodules/renderdoc.pyd` 直接调用 RenderDoc C++ API，利用 `renderdoc.dll` 实现底层图形捕获与回放。